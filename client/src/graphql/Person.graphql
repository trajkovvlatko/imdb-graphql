query Person($id: Int!) {
  person(id: $id) {
    id
    name
    birthYear
    deathYear
    primaryProfession
    titles {
      id
      titleType
      name
      originalName
      isAdult
      startYear
      endYear
      runtimeMinutes
      genres
    }
  }
}
